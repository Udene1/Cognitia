# CI trigger: open-ended experience retrieval

This file exists only to trigger CI for inspection of the open-ended experience-retrieval experiment. The CI artifact must be inspected before any merge.

CI wiring checkpoint: run the open-ended retrieval experiment on every PR change so the retrieval artifact is available for inspection.
